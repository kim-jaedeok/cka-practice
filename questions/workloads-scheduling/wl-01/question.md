Solve this question on: `kubectl config use-context kind-cka`

In namespace `dept-x` the Deployment `api-server` was recently updated to a new
image, but the rollout is failing and Pods from the new ReplicaSet cannot start.

1. Roll the Deployment back to the previous working revision.
2. Scale the Deployment to `4` replicas.
3. All 4 replicas must become Ready.

Do not delete the Deployment.
