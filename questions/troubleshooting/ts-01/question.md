Solve this question on: `kubectl config use-context kind-cka`

In namespace `app-track` the Deployment `frontend` (3 replicas) is not becoming
ready — its Pods are failing to start.

1. Find the root cause.
2. Fix the Deployment so that it runs the intended image **nginx version 1.29**
   and all 3 replicas become Ready.
